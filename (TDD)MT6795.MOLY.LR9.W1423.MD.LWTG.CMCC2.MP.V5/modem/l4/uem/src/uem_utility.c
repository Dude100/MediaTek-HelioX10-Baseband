/*****************************************************************************
*  Copyright Statement:
*  --------------------
*  This software is protected by Copyright and the information contained
*  herein is confidential. The software may not be copied and the information
*  contained herein may not be used or disclosed except with the written
*  permission of MediaTek Inc. (C) 2005
*
*  BY OPENING THIS FILE, BUYER HEREBY UNEQUIVOCALLY ACKNOWLEDGES AND AGREES
*  THAT THE SOFTWARE/FIRMWARE AND ITS DOCUMENTATIONS ("MEDIATEK SOFTWARE")
*  RECEIVED FROM MEDIATEK AND/OR ITS REPRESENTATIVES ARE PROVIDED TO BUYER ON
*  AN "AS-IS" BASIS ONLY. MEDIATEK EXPRESSLY DISCLAIMS ANY AND ALL WARRANTIES,
*  EXPRESS OR IMPLIED, INCLUDING BUT NOT LIMITED TO THE IMPLIED WARRANTIES OF
*  MERCHANTABILITY, FITNESS FOR A PARTICULAR PURPOSE OR NONINFRINGEMENT.
*  NEITHER DOES MEDIATEK PROVIDE ANY WARRANTY WHATSOEVER WITH RESPECT TO THE
*  SOFTWARE OF ANY THIRD PARTY WHICH MAY BE USED BY, INCORPORATED IN, OR
*  SUPPLIED WITH THE MEDIATEK SOFTWARE, AND BUYER AGREES TO LOOK ONLY TO SUCH
*  THIRD PARTY FOR ANY WARRANTY CLAIM RELATING THERETO. MEDIATEK SHALL ALSO
*  NOT BE RESPONSIBLE FOR ANY MEDIATEK SOFTWARE RELEASES MADE TO BUYER'S
*  SPECIFICATION OR TO CONFORM TO A PARTICULAR STANDARD OR OPEN FORUM.
*
*  BUYER'S SOLE AND EXCLUSIVE REMEDY AND MEDIATEK'S ENTIRE AND CUMULATIVE
*  LIABILITY WITH RESPECT TO THE MEDIATEK SOFTWARE RELEASED HEREUNDER WILL BE,
*  AT MEDIATEK'S OPTION, TO REVISE OR REPLACE THE MEDIATEK SOFTWARE AT ISSUE,
*  OR REFUND ANY SOFTWARE LICENSE FEES OR SERVICE CHARGE PAID BY BUYER TO
*  MEDIATEK FOR SUCH MEDIATEK SOFTWARE AT ISSUE. 
*
*  THE TRANSACTION CONTEMPLATED HEREUNDER SHALL BE CONSTRUED IN ACCORDANCE
*  WITH THE LAWS OF THE STATE OF CALIFORNIA, USA, EXCLUDING ITS CONFLICT OF
*  LAWS PRINCIPLES.  ANY DISPUTES, CONTROVERSIES OR CLAIMS ARISING THEREOF AND
*  RELATED THERETO SHALL BE SETTLED BY ARBITRATION IN SAN FRANCISCO, CA, UNDER
*  THE RULES OF THE INTERNATIONAL CHAMBER OF COMMERCE (ICC).
*
*****************************************************************************/

/*****************************************************************************
 *
 * Filename:
 * ---------
 * UEM_UTILITY.C
 *
 * Project:
 * --------
 *   MAUI
 *
 * Description:
 * ------------
 *   This file is intends for UEM utility.
 *
 * Author:
 * -------
 * -------
 *
 *============================================================================
 *             HISTORY
 * Below this line, this part is controlled by PVCS VM. DO NOT MODIFY!!
 *------------------------------------------------------------------------------
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 * removed!
 * removed!
 * removed!
 * removed!
 * removed!
 * removed!
 * removed!
 * removed!
 * removed!
 * removed!
 * removed!
 * removed!
 * removed!
 * removed!
 * removed!
 * removed!
 * removed!
 * removed!
 * removed!
 * removed!
 * removed!
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 * removed!
 * removed!
 * removed!
 *
 *------------------------------------------------------------------------------
 * Upper this line, this part is controlled by PVCS VM. DO NOT MODIFY!!
 *============================================================================
 ****************************************************************************/

/***************************************************************************** 
* Include
*****************************************************************************/
#include "kal_general_types.h"
#include "kal_public_api.h"
#include "kal_public_defs.h" //MSBB change #include "stack_config.h"
#include "ps_public_struct.h"
//#include "stack_ltlcom.h"
#include "nvram_enums.h"
#include "ps_public_enum.h"
#include "nvram_struct.h"
#include "kal_public_defs.h"
#include "custom_em.h"
#include "custom_uem.h"
#include "stdarg.h"
#include "task_config.h"
#include "dcl.h"
#include "l1audio.h"
#include "kal_trace.h"
#include "uem_trc.h"
#include "drv_msgid.h"
#include "svc_sap.h"
#include "md_sap.h"
#include "mw_sap.h"
#include "md_drv_sap.h"
#include "nvram_msgid.h"
#include "em_msgid.h"
#include "sim_ps_msgid.h"
#include "med_msgid.h"
#include "drv_msgid.h"
#include "l4_msgid.h"

//#include "rtc_sw.h"
#include "device.h"
#include "drvsignals.h"

#include "em_struct.h"
#include "ps2sat_struct.h"
#include "med_struct.h"
#include "usb_custom_def.h"

#ifdef __MULTI_BOOT__
#endif 

#ifdef __USB_ENABLE__
#include "usb_comm.h"
#endif 

#include "nvram_data_items.h"
#include "nvram_editor_data_item.h"
#include "nvram_interface.h"

#include "custom_equipment.h"

#include "l4c2uem_struct.h"
#include "l4c_common_enum.h"
#include "l4c_utility.h"

#include "uem_context.h"
#include "uem_utility.h"
#include "uem_proc_cmd.h"

/***************************************************************************** 
* Extern Functions
*****************************************************************************/
extern kal_uint32 custom_cfg_vbat_percentage_convert(kal_uint32 vbat);

/***************************************************************************** 
* Define the audio device mute status
*****************************************************************************/
#define AUDIO_MUTE_SPEAKER          0x01
#define AUDIO_MUTE_MICROPHONE       0x02
#define AUDIO_MUTE_SPEAKER2         0x04
#define AUDIO_MUTE_LOUDSPEAKER      0x08

/***************************************************************************** 
* Local Variables Declaration
*****************************************************************************/
static const kal_uint8 undef_equip_str[MAX_ME_ID_LEN] = "undefined message";

/***************************************************************************** 
* Global Variables Declaration
*****************************************************************************/
UEM_TIMER_EVENTS uem_timer_table[MAX_UEM_EVENT_TIMER];
//stack_timer_struct uem_base_timer;
event_scheduler *event_scheduler_uem_ptr;

#if 0
#if defined(__USB_IN_NORMAL_MODE__) || defined(PICTBRIDGE_SUPPORT)
/* under construction !*/
#if defined(__MTK_TARGET__)
/* under construction !*/
#endif
/* under construction !*/
/* under construction !*/
#endif /* defined(__USB_IN_NORMAL_MODE__) || defined(PICTBRIDGE_SUPPORT) */ 
#endif

#ifdef __CLASSAB_VOLUME_PROTECTION__
const kal_uint32 ADC_MED_QUICK_EVAL_PERIOD  =  KAL_TICKS_1_SEC;  
const kal_uint32 ADC_MED_QUICK_EVAL_COUNT   =  10;
const kal_uint32 ADC_MED_NORMAL_EVAL_PERIOD  =  (2*KAL_TICKS_5_SEC);  
const kal_uint32 ADC_MED_NORMAL_EVAL_COUNT   =  6;
#endif

typedef struct {
    kal_uint8 gain;
} uem_gain_param_struct;

/***************************************************************************** 
****************************************************************************** 
* External Funcations 
******************************************************************************
******************************************************************************/
/*****************************************************************************
*   Function 
*      uem_rtc_alarm_format_convert
*   DESCRIPTION
*      Rtc alarm format convert
*   PARAMETERS
*
*   RETURNS
*      kal_uint8
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_rtc_alarm_format_convert
 * DESCRIPTION
 *  
 * PARAMETERS
 *  rtc_alarm_id        [IN]        
 * RETURNS
 *  
 *****************************************************************************/
kal_uint8 uem_rtc_alarm_format_convert(rtc_alarm_format_enum rtc_alarm_id)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/
    kal_uint8 alarm_format;

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    switch (rtc_alarm_id)
    {
        case DEVICE_AL_EN_None:
            alarm_format = AL_EN_None;
            break;
        case DEVICE_AL_EN_ChkS:
            alarm_format = AL_EN_ChkS;
            break;
        case DEVICE_AL_EN_ChkMS:
            alarm_format = AL_EN_ChkMS;
            break;
        case DEVICE_AL_EN_ChkHMS:
            alarm_format = AL_EN_ChkHMS;
            break;
        case DEVICE_AL_EN_ChkDHMS:
            alarm_format = AL_EN_ChkDHMS;
            break;
        case DEVICE_AL_EN_ChkWHMS:
            alarm_format = AL_EN_ChkWHMS;
            break;
        case DEVICE_AL_EN_ChkMDHMS:
            alarm_format = AL_EN_ChkMDHMS;
            break;
        case DEVICE_AL_EN_ChkYMDHMS:
            alarm_format = AL_EN_ChkYMDHMS;
            break;
        case DEVICE_AL_EN_NoChange:
            alarm_format = AL_EN_NoChange;
            break;
        default:
            alarm_format = AL_EN_None;
            break;
    }

    return alarm_format;
}

/*****************************************************************************
*   Function 
*      uem_rtc_clock_format_convert
*   DESCRIPTION
*      Rtc clock format convert
*   PARAMETERS
*
*   RETURNS
*      kal_uint8
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_rtc_clock_format_convert
 * DESCRIPTION
 *  
 * PARAMETERS
 *  rtc_clock_id        [IN]        
 * RETURNS
 *  
 *****************************************************************************/
kal_uint8 uem_rtc_clock_format_convert(rtc_clock_format_enum rtc_clock_id)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/
    kal_uint8 clock_format;

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    switch (rtc_clock_id)
    {
        case DEVICE_TC_EN_None:
            clock_format = TC_EN_None;
            break;
        case DEVICE_TC_EN_ChkS:
            clock_format = TC_EN_ChkS;
            break;
        case DEVICE_TC_EN_ChkM:
            clock_format = TC_EN_ChkM;
            break;
        case DEVICE_TC_EN_ChkH:
            clock_format = TC_EN_ChkH;
            break;
        case DEVICE_TC_EN_ChkD:
            clock_format = TC_EN_ChkD;
            break;
        case DEVICE_TC_EN_ChkW:
            clock_format = TC_EN_ChkW;
            break;
        case DEVICE_TC_EN_ChkMon:
            clock_format = TC_EN_ChkMon;
            break;
        case DEVICE_TC_EN_ChkY:
            clock_format = TC_EN_ChkY;
            break;
        case DEVICE_TC_EN_NoChange:
            clock_format = TC_EN_NoChange;
            break;
        default:
            clock_format = TC_EN_None;
            break;
    }

    return clock_format;
}

/*****************************************************************************
*   Function 
*      uem_battery_status_convert
*   DESCRIPTION
*      uem battery status convert
*   PARAMETERS
*      vm_status      IN
*   RETURNS
*      dev_status_enum
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_battery_status_convert
 * DESCRIPTION
 *  
 * PARAMETERS
 *  status_type     [IN]        
 * RETURNS
 *  
 *****************************************************************************/
battery_status_enum uem_battery_status_convert(kal_uint8 status_type)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/
    battery_status_enum bs;

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    switch (status_type)
    {
            /* Charger plug in */
        case BMT_CHARGER_IN:
            bs = PMIC_CHARGER_IN;
            break;
            /* Charger plug out */
        case BMT_CHARGER_OUT:
            bs = PMIC_CHARGER_OUT;
            break;
            /* The voltage of battery is too high. */
        case BMT_OVERVOLPROTECT:
            bs = PMIC_OVERVOLPROTECT;
            break;
            /* The temperature of battery is too high. */
        case BMT_OVERBATTEMP:
            bs = PMIC_OVERBATTEMP;
            break;
            /* The temperature of battery is too low. */
        case BMT_LOWBATTEMP:
            bs = PMIC_LOWBATTEMP;
            break;
            /* Charge current is too large. */
        case BMT_OVERCHARGECURRENT:
            bs = PMIC_OVERCHARGECURRENT;
            break;
            /* Charge current is too low. */
        case BMT_LOWCHARGECURRENT:
            bs = PMIC_LOWCHARGECURRENT;
            break;
            /* Charge is completed. */
        case BMT_CHARGE_COMPLETE:
            bs = PMIC_CHARGE_COMPLETE;
            break;
            /* Invalid battery */
        case BMT_INVALID_BAT:
            bs = PMIC_INVALID_BATTERY;
            break;
            /* Invalid charger */
        case BMT_INVALID_CHARGER:
            bs = PMIC_INVALID_CHARGER;
            break;
            /* Charging timeout, battery is bad */
        case BMT_CHARGE_TIMEOUT:
            bs = PMIC_CHARGING_TIMEOUT;
            break;
        case BMT_CHARGE_BAD_CONTACT:
            bs = PMIC_CHARGE_BAD_CONTACT;
            break;
        case BMT_BATTERY_BAD_CONTACT:
            bs = PMIC_BATTERY_BAD_CONTACT;
            break;

    #ifdef __USB_ENABLE__
        case BMT_USB_CHARGER_IN:
            bs = PMIC_USB_CHARGER_IN;
            break;
        case BMT_USB_CHARGER_OUT:
            bs = PMIC_USB_CHARGER_OUT;
            break;
        case BMT_USB_NO_CHARGING_IN:
            bs = PMIC_USB_NO_CHARGER_IN;
            break;
        case BMT_USB_NO_CHARGING_OUT:
            bs = PMIC_USB_NO_CHARGER_OUT;
            break;
    #endif /* __USB_ENABLE__ */ 

        case BMT_BATT_IN:
            bs = PMIC_BATTERY_IN;
            break;
        case BMT_BATT_OUT:
            bs = PMIC_BATTERY_OUT;
            break;            
        default:
            bs = PMIC_VBAT_STATUS;
            break;
    }

    return bs;
}

/*****************************************************************************
*   Function 
*      uem_set_poweron_mode
*   DESCRIPTION
*      Rtc clock format convert
*   PARAMETERS
*      is_key_power      IN
*   RETURNS
*      void
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_set_poweron_mode
 * DESCRIPTION
 *  
 * PARAMETERS
 *  mode        [IN]        
 * RETURNS
 *  void
 *****************************************************************************/
void uem_set_poweron_mode(kal_uint8 mode)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    g_uem_cntx_p->power_on_mode = mode;
}

/*****************************************************************************
*   Function 
*      uem_get_poweron_mode
*   DESCRIPTION
*      Speaker convert
*   PARAMETERS
*      void
*   RETURNS
*      is_key_power      IN
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_get_poweron_mode
 * DESCRIPTION
 *  
 * PARAMETERS
 *  void
 * RETURNS
 *  
 *****************************************************************************/
kal_uint8 uem_get_poweron_mode(void)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    return g_uem_cntx_p->power_on_mode;
}

power_on_status_enum uem_get_power_on_mode(void)
{
    return uem_get_poweron_mode();
}

/*****************************************************************************
*   Function 
*      uem_set_bat_status
*   DESCRIPTION
*      Speaker convert
*   PARAMETERS
*      bat_status      IN
*      bat_vol      IN
*   RETURNS
*      void
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_set_bat_status
 * DESCRIPTION
 *  
 * PARAMETERS
 *  bmt_status      [IN]        
 * RETURNS
 *  void
 *****************************************************************************/
void uem_set_bat_status(uem_bmt_struct bmt_status)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    g_uem_cntx_p->bmt_status.status = bmt_status.status;
    g_uem_cntx_p->bmt_status.volt = bmt_status.volt;
    g_uem_cntx_p->bmt_status.adc = bmt_status.adc;
}

/*****************************************************************************
*   Function 
*      uem_get_bat_status
*   DESCRIPTION
*      Speaker convert
*   PARAMETERS
*      bat_status      IN
*      bat_vol      IN
*   RETURNS
*      void
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_get_bat_status
 * DESCRIPTION
 *  
 * PARAMETERS
 *  bmt_status      [?]     
 * RETURNS
 *  void
 *****************************************************************************/
void uem_get_bat_status(uem_bmt_struct *bmt_status)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    ASSERT(bmt_status);

    bmt_status->status = g_uem_cntx_p->bmt_status.status;
    bmt_status->volt = g_uem_cntx_p->bmt_status.volt;
    bmt_status->adc = g_uem_cntx_p->bmt_status.adc;
}

/*****************************************************************************
*   Function 
*      uem_vbat_level_convert
*   DESCRIPTION
*      vbat convert to user level
*   PARAMETERS
*      bat_status      IN
*      bat_vol      IN
*   RETURNS
*      void
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_vbat_level_convert
 * DESCRIPTION
 *  
 * PARAMETERS
 *  vbat        [IN]        
 * RETURNS
 *  
 *****************************************************************************/
kal_uint8 uem_vbat_level_convert(kal_int32 vbat)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
#ifndef __LTE_RAT__
    return custom_cfg_vbat_level_convert(vbat);
#else
    return 0;
#endif
}

/*****************************************************************************
*   Function 
*      uem_set_silent_mode
*   DESCRIPTION
*      Silent mode
*   PARAMETERS
*      silent_mode      IN
*   RETURNS
*      void
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_set_silent_mode
 * DESCRIPTION
 *  
 * PARAMETERS
 *  silent_mode     [IN]        
 * RETURNS
 *  void
 *****************************************************************************/
void uem_set_silent_mode(kal_bool silent_mode)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    g_uem_cntx_p->rmi_data.silent_mode = silent_mode;
}

/*****************************************************************************
*   Function 
*      uem_get_silent_mode
*   DESCRIPTION
*      Silent mode
*   PARAMETERS
*      silent_mode      IN
*   RETURNS
*      void
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_get_silent_mode
 * DESCRIPTION
 *  
 * PARAMETERS
 *  void
 * RETURNS
 *  
 *****************************************************************************/
kal_uint8 uem_get_silent_mode(void)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    return g_uem_cntx_p->rmi_data.silent_mode;
}

/*****************************************************************************
*   Function 
*      uem_set_alert_mode
*   DESCRIPTION
*      Alert mode
*   PARAMETERS
*      alert_mode      IN
*   RETURNS
*      void
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_set_alert_mode
 * DESCRIPTION
 *  
 * PARAMETERS
 *  alert_mode      [IN]        
 * RETURNS
 *  void
 *****************************************************************************/
void uem_set_alert_mode(kal_uint8 alert_mode)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    g_uem_cntx_p->rmi_data.alert_mode = alert_mode;
}

/*****************************************************************************
*   Function 
*      uem_get_alert_mode
*   DESCRIPTION
*      Alert mode
*   PARAMETERS
*      silent_mode      IN
*   RETURNS
*      void
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_get_alert_mode
 * DESCRIPTION
 *  
 * PARAMETERS
 *  void
 * RETURNS
 *  
 *****************************************************************************/
kal_uint8 uem_get_alert_mode(void)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    return g_uem_cntx_p->rmi_data.alert_mode;
}

/*****************************************************************************
*   Function 
*      uem_set_greeting_text
*   DESCRIPTION
*      Greeting Text
*   PARAMETERS
*      mode      IN
*      greeting_text IN
*   RETURNS
*      void
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_set_greeting_text
 * DESCRIPTION
 *  
 * PARAMETERS
 *  mode                [IN]        
 *  text_dcs            [IN]        
 *  greeting_text       [?]         
 *  no_text             [IN]        
 * RETURNS
 *  void
 *****************************************************************************/
void uem_set_greeting_text(kal_bool mode, kal_uint8 text_dcs, kal_uint8 *greeting_text, kal_uint8 no_text)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    g_uem_cntx_p->rmi_data.greeting_mode = mode;
    g_uem_cntx_p->rmi_data.greeting_text.text_length = no_text;
    g_uem_cntx_p->rmi_data.greeting_text.text_dcs = text_dcs;
    kal_mem_cpy(g_uem_cntx_p->rmi_data.greeting_text.text, greeting_text, UEM_GREETING_LEN - 1);
    g_uem_cntx_p->rmi_data.greeting_text.text[UEM_GREETING_LEN - 1] = '\0';
}

/*****************************************************************************
*   Function 
*      uem_get_alert_mode
*   DESCRIPTION
*      Greeting Text
*   PARAMETERS
*      mode      IN
*      greeting_text IN
*   RETURNS
*      void
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_get_greeting_text
 * DESCRIPTION
 *  
 * PARAMETERS
 *  mode                [?]     
 *  text_dcs            [?]     
 *  greeting_text       [?]     
 *  text_no             [?]     
 * RETURNS
 *  void
 *****************************************************************************/
void uem_get_greeting_text(kal_uint8 *mode, kal_uint8 *text_dcs, kal_uint8 *greeting_text, kal_uint8 *text_no)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    *mode = g_uem_cntx_p->rmi_data.greeting_mode;
    *text_dcs = g_uem_cntx_p->rmi_data.greeting_text.text_dcs;
    *text_no = g_uem_cntx_p->rmi_data.greeting_text.text_length;
    kal_mem_cpy(greeting_text, g_uem_cntx_p->rmi_data.greeting_text.text, UEM_GREETING_LEN);
}

/*****************************************************************************
*   Function 
*      uem_get_equip_id
*   DESCRIPTION
*      Retrieve the string of the equipment ID.
*   PARAMETERS
*      equip_id      IN
*   RETURNS
*      kal_uint8*
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_get_equip_id
 * DESCRIPTION
 *  
 * PARAMETERS
 *  equip_id            [IN]        
 *  equip_string        [?]         
 * RETURNS
 *  void
 *****************************************************************************/
void uem_get_equip_id(kal_uint8 equip_id, kal_uint8 *equip_string)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    switch (equip_id)
    {
        case EQ_ME_REVISION_ID:
    #ifndef _WIN32
        {
            extern kal_char *release_verno(void);

            kal_mem_cpy(equip_string, (kal_uint8*) release_verno(), MAX_ME_ID_LEN - 1);
            equip_string[MAX_ME_ID_LEN - 1] = '\0';
            break;
        }
    #endif /* _WIN32 */ 

        case EQ_ME_MANUFACTURE_ID:
        case EQ_ME_MODEL_ID:
        case EQ_ME_GLOBAL_OBJECT_ID:
        case EQ_ME_MANUFACTURE_INFO_ID:
        case EQ_ME_COMP_CAP_LIST_ID:
        {
            kal_mem_cpy(
                equip_string,
                custom_uem_get_equip_id(equip_id),
                MAX_ME_ID_LEN - 1);
            equip_string[MAX_ME_ID_LEN - 1] = '\0';
            break;
        }
        case EQ_ME_SERIAL_NUMBER_ID:
        {
            kal_uint8 imei_buf[9];

            /* Begin : modify by mtk00714 */
            if (nvram_get_imei_value(8, imei_buf, 1))
            {
                imei_buf[7] = imei_buf[7] | 0xf0;
                /* convert IMEI from BCD to ASCII and copy it into equip_string */
                convert_to_digit(imei_buf, equip_string);
            }
            else
            {   /* get IMEI error */
                equip_string[0] = '\0';
            }
            break;
        }
        case EQ_ME_SERIAL_NUMBER_SIM2_ID:
        {
            kal_uint8 imei_buf[9];

            /* Begin : modify by mtk00714 */
            if (nvram_get_imei_value(8, imei_buf, 2))
            {
                imei_buf[7] = imei_buf[7] | 0xf0;
                /* convert IMEI from BCD to ASCII and copy it into equip_string */
                convert_to_digit(imei_buf, equip_string);
            }
            else
            {   /* get IMEI error */
                equip_string[0] = '\0';
            }
            break;
        }
        default:
            kal_mem_cpy(equip_string, (kal_uint8*) undef_equip_str, MAX_ME_ID_LEN - 1);
            equip_string[MAX_ME_ID_LEN - 1] = '\0';
            break;
    }
}

/*****************************************************************************
*   Function 
*      uem_set_gpio_status
*   DESCRIPTION
*      The function is used to set the level of the GPIO device including the vibrator.
*   PARAMETERS
*      gpio_dev_type      IN
*      gpio_dev_level      IN
*   RETURNS
*      void
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_set_gpio_status
 * DESCRIPTION
 *  
 * PARAMETERS
 *  gpio_dev_type       [IN]        
 *  gpio_dev_level      [IN]        
 * RETURNS
 *  void
 *****************************************************************************/
void uem_set_gpio_status(kal_uint8 gpio_dev_type, kal_uint8 gpio_dev_level)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    if (gpio_dev_type < MAX_GPIO_DEVICE_NUM &&
        gpio_dev_type != GPIO_DEV_LED_MAINLCD)
    {
        g_uem_cntx_p->rmi_data.gpio_status[gpio_dev_type] = gpio_dev_level;
    }
    if (gpio_dev_type == GPIO_DEV_FTE_FEEDBACK_VIB)
    {
        if (uem_get_state() == UEM_STATE_READY)
        {
            g_uem_cntx_p->func_id = L4C_NO_ACTION;
            uem_send_msg_to_nvram(
                MSG_ID_NVRAM_WRITE_REQ,
                NVRAM_EF_UEM_RMI_DATA_LID,
                &(g_uem_cntx_p->rmi_data),
                sizeof(uem_context_rmi_struct));
        }
    }
}

/*****************************************************************************
*   Function 
*      uem_get_gpio_status
*   DESCRIPTION
*      The function is used to get the level of the GPIO device including the vibrator.
*   PARAMETERS
*      gpio_dev_type      IN
*      gpio_dev_level      IN
*   RETURNS
*      void
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_get_gpio_status
 * DESCRIPTION
 *  
 * PARAMETERS
 *  gpio_dev_type       [IN]        
 * RETURNS
 *  
 *****************************************************************************/
kal_uint8 uem_get_gpio_status(kal_uint8 gpio_dev_type)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    return g_uem_cntx_p->rmi_data.gpio_status[gpio_dev_type];
}

/*****************************************************************************
*   Function 
*      uem_audio_set_mute
*   DESCRIPTION
*      
*   PARAMETERS
*      audio_device      IN
*      mute_onoff      IN
*   RETURNS
*      void
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_audio_set_mute
 * DESCRIPTION
 *  
 * PARAMETERS
 *  audio_device        [IN]        
 *  mute_onoff          [IN]        
 * RETURNS
 *  void
 *****************************************************************************/
void uem_audio_set_mute(kal_uint8 audio_device, kal_bool mute_onoff)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    switch (audio_device)
    {
        case AUDIO_DEVICE_MICROPHONE:
            if (mute_onoff)
            {
                g_uem_cntx_p->rmi_data.audio_mute_flag |= AUDIO_MUTE_MICROPHONE;
            }
            else
            {
                g_uem_cntx_p->rmi_data.audio_mute_flag &= ~AUDIO_MUTE_MICROPHONE;
            }
            break;
        case AUDIO_DEVICE_SPEAKER:
            if (mute_onoff)
            {
                g_uem_cntx_p->rmi_data.audio_mute_flag |= AUDIO_MUTE_SPEAKER;
            }
            else
            {
                g_uem_cntx_p->rmi_data.audio_mute_flag &= ~AUDIO_MUTE_SPEAKER;
            }
            break;
        case AUDIO_DEVICE_SPEAKER2:
            if (mute_onoff)
            {
                g_uem_cntx_p->rmi_data.audio_mute_flag |= AUDIO_MUTE_SPEAKER2;
            }
            else
            {
                g_uem_cntx_p->rmi_data.audio_mute_flag &= ~AUDIO_MUTE_SPEAKER2;
            }
            break;
        case AUDIO_DEVICE_LOUDSPEAKER:
            if (mute_onoff)
            {
                g_uem_cntx_p->rmi_data.audio_mute_flag |= AUDIO_MUTE_LOUDSPEAKER;
            }
            else
            {
                g_uem_cntx_p->rmi_data.audio_mute_flag &= ~AUDIO_MUTE_LOUDSPEAKER;
            }
            break;
        default:
            break;
    }
}

/*****************************************************************************
*   Function 
*      uem_audio_get_mute
*   DESCRIPTION
*      
*   PARAMETERS
*      audio_device      IN
*      mute_onoff      IN
*   RETURNS
*      void
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_audio_get_mute
 * DESCRIPTION
 *  
 * PARAMETERS
 *  audio_device        [IN]        
 * RETURNS
 *  
 *****************************************************************************/
kal_bool uem_audio_get_mute(kal_uint8 audio_device)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/
    kal_bool mute_onoff = KAL_FALSE;

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    switch (audio_device)
    {
        case AUDIO_DEVICE_MICROPHONE:
            mute_onoff = ((g_uem_cntx_p->rmi_data.audio_mute_flag) & AUDIO_MUTE_MICROPHONE) ? KAL_TRUE : KAL_FALSE;
            break;
        case AUDIO_DEVICE_SPEAKER:
            mute_onoff = ((g_uem_cntx_p->rmi_data.audio_mute_flag) & AUDIO_MUTE_SPEAKER) ? KAL_TRUE : KAL_FALSE;
            break;
        case AUDIO_DEVICE_SPEAKER2:
            mute_onoff = ((g_uem_cntx_p->rmi_data.audio_mute_flag) & AUDIO_MUTE_SPEAKER2) ? KAL_TRUE : KAL_FALSE;
            break;
        case AUDIO_DEVICE_LOUDSPEAKER:
            mute_onoff = ((g_uem_cntx_p->rmi_data.audio_mute_flag) & AUDIO_MUTE_LOUDSPEAKER) ? KAL_TRUE : KAL_FALSE;
            break;
        default:
            break;
    }

    return mute_onoff;
}

/*****************************************************************************
*   Function 
*      uem_set_datetime_mode
*   DESCRIPTION
*      L4C requests UEM to store date format. Please reference the AT command "+CSDF"
*   PARAMETERS
*      is_date_set      IN
*   RETURNS
*      void
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_set_datetime_format
 * DESCRIPTION
 *  
 * PARAMETERS
 *  is_date_set     [IN]        
 *  format          [IN]        
 * RETURNS
 *  void
 *****************************************************************************/
void uem_set_datetime_format(kal_bool is_date_set, kal_uint8 format)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    /* ref: date_format_enum */
    if (is_date_set == KAL_TRUE)
    {
        g_uem_cntx_p->rmi_data.date_mode = format;
    }
    /* ref: time_format_enum */
    else
    {
        g_uem_cntx_p->rmi_data.time_mode = format;
    }
}

/*****************************************************************************
*   Function 
*      uem_get_datetime_format
*   DESCRIPTION
*      L4C requests UEM to store date format. Please reference the AT command "+CSDF"
*   PARAMETERS
*      is_date_set      IN
*   RETURNS
*      void
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_get_datetime_format
 * DESCRIPTION
 *  
 * PARAMETERS
 *  is_date_set     [IN]        
 * RETURNS
 *  
 *****************************************************************************/
kal_uint8 uem_get_datetime_format(kal_bool is_date_set)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    /* ref: date_format_enum */
    if (is_date_set == KAL_TRUE)
    {
        return g_uem_cntx_p->rmi_data.date_mode;
    }
    /* ref: time_format_enum */
    else
    {
        return g_uem_cntx_p->rmi_data.time_mode;
    }
}

/*****************************************************************************
*   Function 
*      uem_set_state
*   DESCRIPTION
*      NVRAM state
*   PARAMETERS
*      state      IN
*   RETURNS
*      void
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_set_state
 * DESCRIPTION
 *  
 * PARAMETERS
 *  state       [IN]        
 * RETURNS
 *  void
 *****************************************************************************/
void uem_set_state(uem_status_enum state)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    g_uem_cntx_p->state = state;
}

/*****************************************************************************
*   Function 
*      uem_get_state
*   DESCRIPTION
*      NVRAM state
*   PARAMETERS
*      
*   RETURNS
*      NVRAM state
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_get_state
 * DESCRIPTION
 *  
 * PARAMETERS
 *  void
 * RETURNS
 *  
 *****************************************************************************/
kal_uint8 uem_get_state(void)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    return g_uem_cntx_p->state;
}

/*****************************************************************************
*   Function 
*      uem_alarm_set_time
*   DESCRIPTION
*      set alarm time
*   PARAMETERS
*      void
*   RETURNS
*      void
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_alarm_set_time
 * DESCRIPTION
 *  
 * PARAMETERS
 *  rtc_alarm_id        [IN]        
 *  alarm_info          [IN]        
 * RETURNS
 *  void
 *****************************************************************************/
void uem_alarm_set_time(kal_uint8 rtc_alarm_id, rtc_alarm_info_struct alarm_info)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    if (rtc_alarm_id < MAX_ALARM_NUM)
    {
        kal_mem_cpy(&(g_uem_cntx_p->rmi_data.alarm_info[rtc_alarm_id]), &alarm_info, sizeof(rtc_alarm_info_struct));
    }
}

/*****************************************************************************
*   Function 
*      uem_rtc_delete
*   DESCRIPTION
*      
*   PARAMETERS
*      rtc_type      IN
*      rtc_alarm_id   IN
*   RETURNS
*      void
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_rtc_delete
 * DESCRIPTION
 *  
 * PARAMETERS
 *  rtc_type            [IN]        
 *  rtc_alarm_id        [IN]        
 * RETURNS
 *  void
 *****************************************************************************/
void uem_rtc_delete(kal_uint8 rtc_type, kal_uint8 rtc_alarm_id)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    switch (rtc_type)
    {
        case RTC_TIME_CLOCK_IND:
            break;
        case RTC_ALARM_IND:
            if (rtc_alarm_id < MAX_ALARM_NUM)
            {
                g_uem_cntx_p->rmi_data.alarm_info[rtc_alarm_id].alarm_format = DEVICE_TC_EN_None;
            }
            break;
        default:
            break;
    }
}

/*****************************************************************************
*   Function 
*      uem_get_vbat_percentage
*   DESCRIPTION
*      
*   PARAMETERS
*      void
*   RETURNS
*      kal_uint32
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_get_vbat_percentage
 * DESCRIPTION
 *  
 * PARAMETERS
 *  void
 * RETURNS
 *  
 *****************************************************************************/
kal_uint32 uem_get_vbat_percentage(void)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/
    uem_bmt_struct bmt_status;
    kal_uint32 value;

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    uem_get_bat_status(&bmt_status);
#ifndef __LTE_RAT__
    value = custom_cfg_vbat_percentage_convert(bmt_status.volt);
#else
    value = 0;
#endif
    if (value > 10000)
    {
        return 10000;  /* to avoid improper result : > 100% */
    }
    else
    {
        return value;
    }
}

#ifdef __USB_ENABLE__
/*****************************************************************************
*   Function 
*      uem2l4_usb_detect_action_convert
*   DESCRIPTION
*      
*   PARAMETERS
*      action        IN
*   RETURNS
*      kal_uint8
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem2l4_usb_detect_action_convert
 * DESCRIPTION
 *  
 * PARAMETERS
 *  action      [IN]        
 * RETURNS
 *  
 *****************************************************************************/
kal_uint8 uem2l4_usb_detect_action_convert(kal_uint8 action)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/
    USB_DETECT_ACTION_ID usb_action = (USB_DETECT_ACTION_ID) action;

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    switch (usb_action)
    {
        case DETECT_ACTION_PLUGIN:
            return DEVUSB_DETECT_ACTION_PLUGIN;

        case DETECT_ACTION_PLUGOUT:
            return DEVUSB_DETECT_ACTION_PLUGOUT;

        case DETECT_ACTION_USB_SUSPEND:
            return DEVUSB_DETECT_ACTION_USB_SUSPEND;
    
        default:
            return DETECT_ACTION_UNKNOWN;
    }
}

/*****************************************************************************
*   Function 
*      uem2l4_usb_config_action_convert
*   DESCRIPTION
*      
*   PARAMETERS
*      action        IN
*   RETURNS
*      kal_uint8
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem2l4_usb_config_action_convert
 * DESCRIPTION
 *  
 * PARAMETERS
 *  action      [IN]        
 * RETURNS
 *  
 *****************************************************************************/
kal_uint8 uem2l4_usb_config_action_convert(kal_uint8 action)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/
    USB_DEVICE_TYPE usb_action = (USB_DEVICE_TYPE) action;

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    switch (usb_action)
    {
        case USB_CDC_ACM:
            return DEVUSB_CFG_ACTION_CDC_ACM;

        case USB_MASS_STORAGE:
            return DEVUSB_CFG_ACTION_MASS_STORAGE;

        case USB_COMPOSITE_MULTI_COM:
            return DEVUSB_CFG_ACTION_MULTI_COM;

        case USB_VIDEO:
            return DEVUSB_CFG_ACTION_WEBCAM;

        case USB_STOP_MS:
            return DEVUSB_CFG_ACTION_STOP_MS;
            
        case USB_IMAGE:
            return DEVUSB_CFG_ACTION_PICTBRIGE;
        case USB_IMAGE_MTP:
            return DEVUSB_CFG_ACTION_MTP;

        case USB_STOP_MTP:
            return DEVUSB_CFG_ACTION_STOP_MTP;
        
        default:
            return DEVUSB_CFG_ACTION_UNKNOWN;
    }
}

/*****************************************************************************
*   Function 
*      uem2drv_usb_config_action_convert
*   DESCRIPTION
*      
*   PARAMETERS
*      action        IN
*   RETURNS
*      kal_uint8
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem2drv_usb_config_action_convert
 * DESCRIPTION
 *  
 * PARAMETERS
 *  action      [IN]        
 * RETURNS
 *  
 *****************************************************************************/
kal_uint8 uem2drv_usb_config_action_convert(kal_uint8 action)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/
    devusb_cfg_action_enum usb_action = (devusb_cfg_action_enum) action;

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    switch (usb_action)
    {
        case DEVUSB_CFG_ACTION_CDC_ACM:
            return USB_CDC_ACM;

        case DEVUSB_CFG_ACTION_MASS_STORAGE:
            return USB_MASS_STORAGE;

        case DEVUSB_CFG_ACTION_WEBCAM:
            return USB_VIDEO;

        case DEVUSB_CFG_ACTION_MULTI_COM:
            return USB_COMPOSITE_MULTI_COM;   

        case DEVUSB_CFG_ACTION_STOP_MS:
            return USB_STOP_MS;

        case DEVUSB_CFG_ACTION_PICTBRIGE:
            return USB_IMAGE;
        case DEVUSB_CFG_ACTION_MTP:
            return USB_IMAGE_MTP;
			
        case DEVUSB_CFG_ACTION_STOP_MTP:
            return USB_STOP_MTP;

        default:
            return USB_UNKOWN;
    }
}
#endif /* __USB_ENABLE__ */ 

#if defined(PLUTO_MMI)
/*****************************************************************************
*   Function 
*      is_status_matched
*   DESCRIPTION
*      
*   PARAMETERS
*      
*   RETURNS
*      
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  is_status_matched
 * DESCRIPTION
 *  
 * PARAMETERS
 *  type        [IN]        
 * RETURNS
 *  
 *****************************************************************************/
kal_bool is_status_matched(uem_accessory_status_enum type)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    if (FLAG_IS_ON(g_uem_cntx_p->acc_prev_state, type) == FLAG_IS_ON(g_uem_cntx_p->acc_latest_state, type))
    {
        return KAL_TRUE;
    }
    else
    {
        return KAL_FALSE;
    }
}
#endif /* defined(PLUTO_MMI) */ 

/*****************************************************************************
*   Function 
*      uem_send_msg_to_l4c
*   DESCRIPTION
*      
*   PARAMETERS
*      msg_name        IN
*      data_ptr        IN
*   RETURNS
*      void
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_send_msg_to_l4c
 * DESCRIPTION
 *  
 * PARAMETERS
 *  msg_name        [IN]        
 *  data_ptr        [?]         
 * RETURNS
 *  void
 *****************************************************************************/
void uem_send_msg_to_l4c(msg_type msg_name, void *data_ptr)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/
    kal_bool ret_val = KAL_TRUE;
    void *local_data_ptr;
    local_para_struct *local_para_ptr = NULL;
    peer_buff_struct *peer_buff_ptr = NULL;

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    kal_trace(TRACE_FUNC, FUNC_UEM_SEND_MSG_TO_L4C);

    switch (msg_name)
    {
        #if 0
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
        #endif
        case MSG_ID_L4CUEM_SET_HW_LEVEL_CNF:
            local_data_ptr =
                (l4cuem_set_hw_level_cnf_struct*) construct_local_para(
                                                    sizeof(l4cuem_set_hw_level_cnf_struct),
                                                    TD_CTRL);
            ((l4cuem_set_hw_level_cnf_struct*) local_data_ptr)->src_id = g_uem_cntx_p->src_id;

            if (g_uem_cntx_p->cause_id == NVRAM_IO_ERRNO_OK)
            {
                ((l4cuem_set_hw_level_cnf_struct*) local_data_ptr)->response = L4C_OK;
            }
            else
            {
                ((l4cuem_set_hw_level_cnf_struct*) local_data_ptr)->response = L4C_ERROR;
            }

            ((l4cuem_set_hw_level_cnf_struct*) local_data_ptr)->cause = g_uem_cntx_p->cause_id;

            local_para_ptr = (local_para_struct*) local_data_ptr;
            break;

            /* set startup confirm to L4c */
        case MSG_ID_L4CUEM_STARTUP_CNF:
            local_data_ptr =
                (l4cuem_startup_cnf_struct*) construct_local_para(sizeof(l4cuem_startup_cnf_struct), TD_CTRL);
            ((l4cuem_startup_cnf_struct*) local_data_ptr)->src_id = LMMI_SRC;

            if (g_uem_cntx_p->cause_id == NVRAM_IO_ERRNO_OK)
            {
                ((l4cuem_startup_cnf_struct*) local_data_ptr)->response = L4C_OK;
            }
            else
            {
                ((l4cuem_startup_cnf_struct*) local_data_ptr)->response = L4C_ERROR;
            }

            ((l4cuem_startup_cnf_struct*) local_data_ptr)->cause = g_uem_cntx_p->cause_id;

            local_para_ptr = (local_para_struct*) local_data_ptr;
            break;

        case MSG_ID_L4CUEM_SET_AUDIO_PROFILE_CNF:
        case MSG_ID_L4CUEM_SET_AUDIO_PARAM_CNF:
        case MSG_ID_L4CUEM_AUDIO_PLAY_BY_NAME_CNF:
        case MSG_ID_L4CUEM_AUDIO_STOP_BY_NAME_CNF:
        case MSG_ID_L4CUEM_AUDIO_PLAY_BY_STRING_CNF:
        case MSG_ID_L4CUEM_AUDIO_STOP_BY_STRING_CNF:
            /* hold the local parameter struct and directly pass to L4c */
            hold_local_para((local_para_struct*) data_ptr);
            local_para_ptr = (local_para_struct*) data_ptr;
            break;

        case MSG_ID_L4CPS_EM_STATUS_IND:
            /* Engineering mode idication: send status to L4C-EM module */
        {
            peer_buff_struct *peer_ptr;
            l4cps_em_status_ind_struct *local_ptr;
            kal_uint16 len_pdu;
            uem_em_battery_info *l_uem_battery_info;
            uem_bmt_struct bmt_status;

            /* get the battery status */
            uem_get_bat_status(&bmt_status);

            /* fill the battery status */
            peer_ptr = (peer_buff_struct*) construct_peer_buff(sizeof(uem_em_battery_info), 0, 0, TD_CTRL);
            l_uem_battery_info = (uem_em_battery_info*) get_peer_buff_pdu(peer_ptr, &(len_pdu));
            l_uem_battery_info->adc_value = bmt_status.volt;
            l_uem_battery_info->battery_level = uem_vbat_level_convert(bmt_status.volt);
            l_uem_battery_info->battery_status = bmt_status.status;

            /* fill the UEM battery infomation indication */
            local_ptr =
                (l4cps_em_status_ind_struct*) construct_local_para(sizeof(l4cps_em_status_ind_struct), TD_CTRL);
            local_ptr->em_info = UEM_EM_BATTERY_INFO;

            /* fill the message */
            local_para_ptr = (local_para_struct*) local_ptr;
            peer_buff_ptr = (peer_buff_struct*) peer_ptr;
            break;
        }

        default:
            ret_val = KAL_FALSE;
            /* SME_ASSERT(KAL_FALSE, "Wrong message" ); */
            break;
    }   /* End Switch */
    /* Check whether that there were no errors while constructing the message.  */

    /* Send the message to the UEM module */
    if (ret_val == KAL_TRUE)
    {
//        SEND_ILM(MOD_UEM, MOD_L4C, UEM_L4C_SAP, (&ilm_ptr));
        msg_send6(MOD_UEM, MOD_L4C, UEM_L4C_SAP, msg_name, local_para_ptr, peer_buff_ptr);
    }

    return;
}

/*****************************************************************************
*   Function 
*      uem_send_msg_to_nvram
*   DESCRIPTION
*      
*   PARAMETERS
*      msg_name        IN
*      ef_id           IN
*      data_ptr        IN
*      length        IN
*   RETURNS
*      void
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_send_msg_to_nvram
 * DESCRIPTION
 *  
 * PARAMETERS
 *  msg_name        [IN]        
 *  ef_id           [IN]        
 *  data_ptr        [?]         
 *  length          [IN]        
 * RETURNS
 *  void
 *****************************************************************************/
void uem_send_msg_to_nvram(msg_type msg_name, kal_uint16 ef_id, void *data_ptr, kal_uint16 length)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/
    kal_bool ret_val = KAL_TRUE;
    peer_buff_struct *data_stream;
    local_para_struct *parm_stream;
    kal_uint16 pdu_len;
    local_para_struct *local_para_ptr = NULL;
    peer_buff_struct *peer_buff_ptr = NULL;

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    kal_trace(TRACE_FUNC, FUNC_UEM_SEND_MSG_TO_NVRAM);

    switch (msg_name)
    {
        case MSG_ID_NVRAM_WRITE_REQ:
            switch (ef_id)
            {
                case NVRAM_EF_UEM_RMI_DATA_LID:
                case NVRAM_EF_CUST_HW_LEVEL_TBL_LID:
                case NVRAM_EF_PORT_SETTING_LID:
                    parm_stream = construct_local_para(sizeof(nvram_write_req_struct), TD_CTRL);
                    data_stream = construct_peer_buff(length, 0, 0, TD_CTRL);

                    ((nvram_write_req_struct*) parm_stream)->file_idx = (kal_uint16) ef_id;
                    ((nvram_write_req_struct*) parm_stream)->para = 1;
                    /* keep the function id in NVRAM message from UEM */
                    ((nvram_write_req_struct*) parm_stream)->access_id = g_uem_cntx_p->func_id;

                    pdu_len = length;
                    kal_mem_cpy(get_peer_buff_pdu(data_stream, &pdu_len), data_ptr, length);

                    local_para_ptr = (local_para_struct*) parm_stream;
                    peer_buff_ptr = (peer_buff_struct*) data_stream;
                    break;
                default:
                    /* error write */
                    break;
            }
            break;

        case MSG_ID_NVRAM_READ_REQ:
            switch (ef_id)
            {
                case NVRAM_EF_UEM_RMI_DATA_LID:
                case NVRAM_EF_UEM_MANUFACTURE_DATA_LID:
                case NVRAM_EF_CUST_HW_LEVEL_TBL_LID:
                case NVRAM_EF_PORT_SETTING_LID:
                case NVRAM_EF_CAL_FLAG_LID:
            #ifdef __TC01__
                case NVRAM_EF_RF_CAL_ENV_LID:
                case NVRAM_EF_CAL_DATA_CHECK_LID:
            #endif
                    parm_stream = construct_local_para(sizeof(nvram_read_req_struct), TD_CTRL);

                    ((nvram_read_req_struct*) parm_stream)->file_idx = (kal_uint16) ef_id;
                    ((nvram_read_req_struct*) parm_stream)->para = 1;

                    local_para_ptr = (local_para_struct*) parm_stream;
                    break;

                default:
                    /* error read */
                    break;
            }
            break;

        default:
            ret_val = KAL_FALSE;
            /* SME_ASSERT(KAL_FALSE, "Wrong message" ); */
            break;
    }   /* End Switch */
    /* Check whether that there were no errors while constructing the message.  */

    /* Send the message to the NVRAM module */
    if (ret_val == KAL_TRUE)
    {
//        SEND_ILM(MOD_UEM, MOD_NVRAM, PS_NVRAM_SAP, (&ilm_ptr));
        msg_send6(MOD_UEM, MOD_NVRAM, PS_NVRAM_SAP, msg_name, local_para_ptr, peer_buff_ptr);
    }

    return;
}

/*****************************************************************************
*   Function 
*      uem_send_msg_to_sat
*   DESCRIPTION
*      
*   PARAMETERS
*      ...
*   RETURNS
*      void
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_send_msg_to_sat
 * DESCRIPTION
 *  
 * PARAMETERS
 *  msg_name        [IN]        
 *  data_ptr        [?]         
 * RETURNS
 *  void
 *****************************************************************************/
void uem_send_msg_to_sat(kal_uint32 src_mod_id, msg_type msg_name, void *data_ptr)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/
    kal_bool ret_val = KAL_TRUE;
    sat_language_cnf_struct *l_language_cnf_data;
    sat_current_time_cnf_struct *l_current_time_cnf_data;
    local_para_struct *local_para_ptr = NULL;

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    kal_trace(TRACE_FUNC, FUNC_UEM_SEND_MSG_TO_SAT);

    switch (msg_name)
    {
        case MSG_ID_SAT_LANGUAGE_CNF:
            l_language_cnf_data = (sat_language_cnf_struct*)
                construct_local_para(sizeof(sat_language_cnf_struct), TD_CTRL);

            l_language_cnf_data->language[0] = g_uem_cntx_p->rmi_data.lang_id[0];
            l_language_cnf_data->language[1] = g_uem_cntx_p->rmi_data.lang_id[1];

            local_para_ptr = (local_para_struct*) l_language_cnf_data;
            break;

        case MSG_ID_SAT_CURRENT_TIME_CNF:
        {
            rtc_alarm_info_struct l_rtc_clock_info;

            l_current_time_cnf_data = (sat_current_time_cnf_struct*)
                construct_local_para(sizeof(sat_current_time_cnf_struct), TD_CTRL);

            l4cuem_rtc_get_time_req(RTC_TIME_CLOCK_IND, 0, &l_rtc_clock_info);

            l_current_time_cnf_data->time_info[0] = l_rtc_clock_info.data_time.rtc_day;
            l_current_time_cnf_data->time_info[1] = l_rtc_clock_info.data_time.rtc_hour;
            l_current_time_cnf_data->time_info[2] = l_rtc_clock_info.data_time.rtc_min;
            l_current_time_cnf_data->time_info[3] = l_rtc_clock_info.data_time.rtc_sec;
            l_current_time_cnf_data->time_info[4] = l_rtc_clock_info.data_time.rtc_year;
            l_current_time_cnf_data->time_info[5] = l_rtc_clock_info.data_time.rtc_mon;

            local_para_ptr = (local_para_struct*) l_current_time_cnf_data;
            break;
        }

        default:
            ret_val = KAL_FALSE;
            /* SME_ASSERT(KAL_FALSE, "Wrong message" ); */
            break;
    }   /* End Switch */
    /* Check whether that there were no errors while constructing the message.  */

    /* Send the message to the UEM module */
    if (ret_val == KAL_TRUE)
    {
//        SEND_ILM(MOD_UEM, (module_type)src_mod_id, PS_SIM_SAP, (&ilm_ptr));
        msg_send5(MOD_UEM, (module_type)src_mod_id, PS_SIM_SAP, msg_name, local_para_ptr);
    }

    return;
}

/*****************************************************************************
*   Function 
*      uem_send_msg_to_aud
*   DESCRIPTION
*      
*   PARAMETERS
*      ...
*   RETURNS
*      void
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_send_msg_to_aud
 * DESCRIPTION
 *  
 * PARAMETERS
 *  msg_name        [IN]        
 * RETURNS
 *  void
 *****************************************************************************/
void uem_send_msg_to_aud(kal_uint32 msg_name, ...)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/
#if 1 // !defined(MED_NOT_PRESENT)    
    va_list varpars;
    kal_bool ret_val = KAL_TRUE;
    local_para_struct *local_para_ptr = NULL;

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    kal_trace(TRACE_FUNC, FUNC_UEM_SEND_MSG_TO_AUD);

    switch (msg_name)
    {
        case MSG_ID_MED_STARTUP_REQ:
        {
            med_startup_req_struct *dst_data_ptr;

            dst_data_ptr = (med_startup_req_struct*) construct_local_para(sizeof(med_startup_req_struct), TD_CTRL);

            local_para_ptr = (local_para_struct*) dst_data_ptr;
            break;
        }
        case MSG_ID_MEDIA_AUD_SET_AUDIO_MODE_REQ:
        {
            media_aud_set_audio_mode_req_struct *dst_data_ptr;

            dst_data_ptr = (media_aud_set_audio_mode_req_struct*)
                construct_local_para(sizeof(media_aud_set_audio_mode_req_struct), TD_CTRL);

            va_start(varpars, msg_name);
            dst_data_ptr->mode = (kal_uint8) va_arg(varpars, kal_uint32);
            va_end(varpars);

            local_para_ptr = (local_para_struct*) dst_data_ptr;
            break;
        }
        /* JhZhao HAL*/
        #if 0
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
        #endif
        #if 0
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
        #endif
        case MSG_ID_MEDIA_AUD_SP_SET_MODE_REQ:
        {
            meida_aud_sp_set_mode_req_struct *dst_data_ptr;

            dst_data_ptr = (meida_aud_sp_set_mode_req_struct*)
                construct_local_para(sizeof(meida_aud_sp_set_mode_req_struct), TD_CTRL);

            va_start(varpars, msg_name);
            dst_data_ptr->speech_on = (kal_bool) va_arg(varpars, kal_uint32);
            dst_data_ptr->rat_mode = (kal_uint8) va_arg(varpars, kal_uint32);
            va_end(varpars);

            local_para_ptr = (local_para_struct*) dst_data_ptr;
            break;
        }
        case MSG_ID_MEDIA_AUD_SET_DEVICE_REQ:
        {
            media_aud_set_device_req_struct *dst_data_ptr;

            dst_data_ptr = (media_aud_set_device_req_struct*)
                construct_local_para(sizeof(media_aud_set_device_req_struct), TD_CTRL);

            va_start(varpars, msg_name);
            dst_data_ptr->audio_type = (kal_uint8) va_arg(varpars, kal_uint32);
            dst_data_ptr->device = (kal_uint8) va_arg(varpars, kal_uint32);
            va_end(varpars);

            local_para_ptr = (local_para_struct*) dst_data_ptr;
            break;
        }
        case MSG_ID_MEDIA_AUD_PLAY_BY_ID_REQ:
        {
            media_aud_play_by_id_req_struct *dst_data_ptr;

            dst_data_ptr = (media_aud_play_by_id_req_struct*)
                construct_local_para(sizeof(media_aud_play_by_id_req_struct), TD_CTRL);

            va_start(varpars, msg_name);
            dst_data_ptr->audio_id = (kal_uint8) va_arg(varpars, kal_uint32);
            dst_data_ptr->play_style = (kal_uint8) va_arg(varpars, kal_uint32);
            dst_data_ptr->identifier = (kal_uint16) va_arg(varpars, kal_uint32);
            dst_data_ptr->volume = (kal_uint64) va_arg(varpars, kal_uint64);
            dst_data_ptr->output_path = (kal_uint8) va_arg(varpars, kal_uint32);
            
            va_end(varpars);

            local_para_ptr = (local_para_struct*) dst_data_ptr;
            break;
        }
        case MSG_ID_MEDIA_AUD_STOP_BY_ID_REQ:
        {
            media_aud_stop_by_id_req_struct *dst_data_ptr;

            dst_data_ptr = (media_aud_stop_by_id_req_struct*)
                construct_local_para(sizeof(media_aud_stop_by_id_req_struct), TD_CTRL);

            va_start(varpars, msg_name);
            dst_data_ptr->audio_id = (kal_uint8) va_arg(varpars, kal_uint32);
            va_end(varpars);

            local_para_ptr = (local_para_struct*) dst_data_ptr;
            break;
        }
        case MSG_ID_MEDIA_AUD_MUTE_REQ:
        {
            media_aud_mute_req_struct *dst_data_ptr;

            dst_data_ptr = (media_aud_mute_req_struct*)
                construct_local_para(sizeof(media_aud_mute_req_struct), TD_CTRL);

            va_start(varpars, msg_name);
            dst_data_ptr->device = (kal_uint8) va_arg(varpars, kal_uint32);
            dst_data_ptr->mute = (kal_bool) va_arg(varpars, kal_uint32);
            va_end(varpars);

            local_para_ptr = (local_para_struct*) dst_data_ptr;
            break;
        }
        case MSG_ID_MEDIA_AUD_SET_VOLUME_REQ:
        {
            media_aud_set_volume_req_struct *dst_data_ptr;

            dst_data_ptr = (media_aud_set_volume_req_struct*)
                construct_local_para(sizeof(media_aud_set_volume_req_struct), TD_CTRL);

            va_start(varpars, msg_name);
            dst_data_ptr->audio_type = (kal_uint8) va_arg(varpars, kal_uint32);
            dst_data_ptr->volume = (kal_uint8) va_arg(varpars, kal_uint32);
            dst_data_ptr->aux_volume = (kal_uint8) va_arg(varpars, kal_uint32);
            dst_data_ptr->mode = (kal_uint8) va_arg(varpars, kal_uint32);
            va_end(varpars);

            local_para_ptr = (local_para_struct*) dst_data_ptr;
            break;
        }
    /* JhZhao HAL*/
#if 0
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
#endif
        case MSG_ID_MEDIA_AUD_PLAY_BY_FILE_REQ:
        case MSG_ID_MEDIA_AUD_STOP_REQ:
        case MSG_ID_MEDIA_AUD_PLAY_BY_STRING_REQ:
        case MSG_ID_MEDIA_AUD_STOP_BY_STRING_REQ:
        case MSG_ID_MEDIA_AUD_SET_AUDIO_PROFILE_REQ:
        case MSG_ID_MEDIA_AUD_SET_AUDIO_PARAM_REQ:
        {
            local_para_struct *data_ptr;

            va_start(varpars, msg_name);
            data_ptr = (local_para_struct*) va_arg(varpars, kal_uint32);
            va_end(varpars);

            /* hold the local parameter struct and directly pass to L4c */
            hold_local_para((local_para_struct*) data_ptr);

            local_para_ptr = (local_para_struct*) data_ptr;
            break;
        }
#if 0
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
#endif //0
        case MSG_ID_MEDIA_AUD_GET_AUDIO_PARAM_REQ:
        {
            media_aud_get_audio_param_req_struct *data_ptr;

            data_ptr = (media_aud_get_audio_param_req_struct*)
                construct_local_para(sizeof(media_aud_get_audio_param_req_struct), TD_CTRL);

            va_start(varpars, msg_name);
            data_ptr->src_id = (kal_uint8) va_arg(varpars, kal_uint32);
            va_end(varpars);

            local_para_ptr = (local_para_struct*) data_ptr;

            break;
        }
        case MSG_ID_MEDIA_AUD_GET_AUDIO_PROFILE_REQ:
        {
            media_aud_get_audio_profile_req_struct *data_ptr;

            data_ptr = (media_aud_get_audio_profile_req_struct*)
                construct_local_para(sizeof(media_aud_get_audio_profile_req_struct), TD_CTRL);

            va_start(varpars, msg_name);
            data_ptr->mode = (kal_int8) va_arg(varpars, kal_uint32);
            data_ptr->src_id = (kal_uint8) va_arg(varpars, kal_uint32);
            va_end(varpars);

            local_para_ptr = (local_para_struct*) data_ptr;
            break;
        }
        case MSG_ID_MEDIA_AUD_GET_VOLUME_REQ:
        {
            media_aud_get_volume_req_struct *dst_data_ptr;

            dst_data_ptr = (media_aud_get_volume_req_struct*)
                construct_local_para(sizeof(media_aud_get_volume_req_struct), TD_CTRL);


            va_start(varpars, msg_name);
            dst_data_ptr->volume_type = (kal_int16) va_arg(varpars, kal_uint32);
            dst_data_ptr->src_id = (kal_uint8) va_arg(varpars, kal_uint32);
            va_end(varpars);

            local_para_ptr = (local_para_struct*) dst_data_ptr;
            break;
        }
        #if 0
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
        #endif
    #ifdef __CLASSAB_VOLUME_PROTECTION__
        case MSG_ID_L4AUD_BATTERY_VOLTAGE_IND:
        {
            l4audio_batt_voltage_ind_struct *dst_data_ptr;

            dst_data_ptr = (l4audio_batt_voltage_ind_struct*)
                construct_local_para(sizeof(l4audio_batt_voltage_ind_struct), TD_CTRL);

            va_start(varpars, msg_name);
            dst_data_ptr->voltage = (kal_uint32) va_arg(varpars, kal_uint32);
            va_end(varpars);

            local_para_ptr = (local_para_struct*) dst_data_ptr;
            break;
        }
    #endif

#if defined(__AMRWB_LINK_SUPPORT__)
        case MSG_ID_MEDIA_AUD_GET_AUDIO_WB_INPUT_FIR_PARAM_REQ:
        {
            media_aud_get_audio_wb_input_fir_param_req_struct *dst_data_ptr;

            dst_data_ptr = (media_aud_get_audio_wb_input_fir_param_req_struct *)
                    construct_local_para(sizeof(media_aud_get_audio_wb_input_fir_param_req_struct),TD_CTRL);
            va_start(varpars, msg_name);
            dst_data_ptr->src_id = (kal_uint8)va_arg(varpars, kal_uint32);
            va_end(varpars);

            local_para_ptr = (local_para_struct*) dst_data_ptr;
            break;
        }
        case MSG_ID_MEDIA_AUD_GET_AUDIO_WB_OUTPUT_FIR_PARAM_REQ:
        {
            media_aud_get_audio_wb_output_fir_param_req_struct *dst_data_ptr;

            dst_data_ptr = (media_aud_get_audio_wb_output_fir_param_req_struct *)
                    construct_local_para(sizeof(media_aud_get_audio_wb_output_fir_param_req_struct),TD_CTRL);
            va_start(varpars, msg_name);
            dst_data_ptr->src_id = (kal_uint8)va_arg(varpars, kal_uint32);
            va_end(varpars);

            local_para_ptr = (local_para_struct*) dst_data_ptr;
            break;
        }
        case MSG_ID_MEDIA_AUD_GET_AUDIO_WB_MODE_PARAM_REQ:
        {
            media_aud_get_audio_wb_mode_param_req_struct *dst_data_ptr;

            dst_data_ptr = (media_aud_get_audio_wb_mode_param_req_struct *)
                    construct_local_para(sizeof(media_aud_get_audio_wb_mode_param_req_struct),TD_CTRL);
            va_start(varpars, msg_name);
            dst_data_ptr->src_id = (kal_uint8)va_arg(varpars, kal_uint32);
            va_end(varpars);

            local_para_ptr = (local_para_struct*) dst_data_ptr;
            break;
        }
        case MSG_ID_MEDIA_AUD_SET_AUDIO_WB_INPUT_FIR_PARAM_REQ:
        {
            media_aud_set_audio_wb_input_fir_param_req_struct *dst_data_ptr;
            audio_wb_speech_fir_struct *new_fir;

            dst_data_ptr = (media_aud_set_audio_wb_input_fir_param_req_struct *)
                    construct_local_para(sizeof(media_aud_set_audio_wb_input_fir_param_req_struct), TD_CTRL);
            va_start(varpars, msg_name);
            dst_data_ptr->src_id = (kal_uint8)va_arg(varpars, kal_uint32);
            new_fir = (audio_wb_speech_fir_struct *)va_arg(varpars, kal_uint32);
            va_end(varpars);
            dst_data_ptr->update_nvram = KAL_TRUE;
            kal_mem_cpy((void *)&(dst_data_ptr->fir_param), new_fir, sizeof(audio_wb_speech_fir_struct));
            local_para_ptr = (local_para_struct*) dst_data_ptr;
            
            break;
            
        }
        case MSG_ID_MEDIA_AUD_SET_AUDIO_WB_OUTPUT_FIR_PARAM_REQ:
        {
            media_aud_set_audio_wb_output_fir_param_req_struct *dst_data_ptr;
            audio_wb_speech_fir_struct *new_fir;

            dst_data_ptr = (media_aud_set_audio_wb_output_fir_param_req_struct *)
                    construct_local_para(sizeof(media_aud_set_audio_wb_output_fir_param_req_struct), TD_CTRL);
            va_start(varpars, msg_name);
            dst_data_ptr->src_id = (kal_uint8)va_arg(varpars, kal_uint32);
            new_fir = (audio_wb_speech_fir_struct *)va_arg(varpars, kal_uint32);
            va_end(varpars);
            dst_data_ptr->update_nvram = KAL_TRUE;
            kal_mem_cpy((void *)&(dst_data_ptr->fir_param), new_fir, sizeof(audio_wb_speech_fir_struct));
            local_para_ptr = (local_para_struct*) dst_data_ptr;
            
            break;
            
        }
        case MSG_ID_MEDIA_AUD_SET_AUDIO_WB_MODE_PARAM_REQ:
        {
            media_aud_set_audio_wb_mode_param_req_struct *dst_data_ptr;
            audio_wb_speech_mode_struct *new_mode;

            dst_data_ptr = (media_aud_set_audio_wb_mode_param_req_struct *)
                    construct_local_para(sizeof(media_aud_set_audio_wb_mode_param_req_struct), TD_CTRL);
            
            va_start(varpars, msg_name);
            dst_data_ptr->src_id = (kal_uint8)va_arg(varpars, kal_uint32);
            new_mode = (audio_wb_speech_mode_struct *)va_arg(varpars, kal_uint32);
            va_end(varpars);
            dst_data_ptr->update_nvram = KAL_TRUE;
            kal_mem_cpy((void *)&(dst_data_ptr->mode), new_mode, sizeof(audio_wb_speech_mode_struct));
            local_para_ptr = (local_para_struct*) dst_data_ptr;
            
            break;
        }
#endif
        default:
            ret_val = KAL_FALSE;
            break;
    }   /* End Switch */
    /* Check whether that there were no errors while constructing the message. */

    /* Send the message to the AUD module */
    if (ret_val == KAL_TRUE)
    {
//        SEND_ILM(MOD_UEM, MOD_MED, MED_SAP, (&ilm_ptr));
        msg_send5(MOD_UEM, MOD_MED, MED_SAP, (msg_type)msg_name, local_para_ptr);
    }

    return;
#endif /* #if defined(MED_NOT_PRESENT) */    
}

/*****************************************************************************
*   Function 
*      uem_send_msg_to_bmt
*   DESCRIPTION
*      
*   PARAMETERS
*      ...
*   RETURNS
*      void
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_send_msg_to_bmt
 * DESCRIPTION
 *  
 * PARAMETERS
 *  msg_name        [IN]        
 * RETURNS
 *  void
 *****************************************************************************/
void uem_send_msg_to_bmt(kal_uint32 msg_name, ...)
{
#ifdef __MTK_TARGET__
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/
    kal_bool ret_val = KAL_TRUE;
    void *parm_stream;
    va_list varpars;
    local_para_struct *local_para_ptr = NULL;

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    kal_trace(TRACE_FUNC, FUNC_UEM_SEND_MSG_TO_BMT);

    switch (msg_name)
    {
        case MSG_ID_BMT_ADC_ADD_ITEM_REQ:
        {
            parm_stream =
                (bmt_adc_add_item_req_struct*) construct_local_para(sizeof(bmt_adc_add_item_req_struct), TD_CTRL);

            va_start(varpars, msg_name);
            ((bmt_adc_add_item_req_struct*) parm_stream)->adc_handle = (DCL_HANDLE) va_arg(varpars, kal_uint32);
            va_end(varpars);

            local_para_ptr = (local_para_struct*) parm_stream;
            break;
        }
        case MSG_ID_BMT_ADC_REMOVE_ITEM_REQ:
        {
            parm_stream =
                (bmt_adc_remove_item_req_struct*) construct_local_para(
                                                    sizeof(bmt_adc_remove_item_req_struct),
                                                    TD_CTRL);

            va_start(varpars, msg_name);
            ((bmt_adc_remove_item_req_struct*) parm_stream)->adc_handle = (DCL_HANDLE) va_arg(varpars, kal_uint32);
            va_end(varpars);

            local_para_ptr = (local_para_struct*) parm_stream;
            break;
        }
        case MSG_ID_BMT_ADC_MODIFY_PARAMETERS_REQ:
        {
            parm_stream =
                (bmt_adc_modify_parameters_req_struct*)
                construct_local_para(sizeof(bmt_adc_modify_parameters_req_struct), TD_CTRL);

            va_start(varpars, msg_name);
            ((bmt_adc_modify_parameters_req_struct*) parm_stream)->adc_handle =
                (DCL_HANDLE) va_arg(varpars, kal_uint32);
            ((bmt_adc_modify_parameters_req_struct*) parm_stream)->evaluate_count =
                (kal_uint8) va_arg(varpars, kal_uint32);
            ((bmt_adc_modify_parameters_req_struct*) parm_stream)->period = (kal_uint32) va_arg(varpars, kal_uint32);
            va_end(varpars);

            local_para_ptr = (local_para_struct*) parm_stream;
            break;
        }
        default:
            ret_val = KAL_FALSE;
            break;
    }

    /* Send the message to the UEM module */
    if (ret_val == KAL_TRUE)
    {
//        SEND_ILM(MOD_UEM, MOD_BMT, DRIVER_PS_SAP, (&ilm_ptr));
        msg_send5(MOD_UEM, MOD_BMT, DRIVER_PS_SAP, (msg_type)msg_name, local_para_ptr);
    }
#endif /* __MTK_TARGET__ */ 

    return;
}

/*****************************************************************************
*   Function 
*      uem_send_msg_to_aux
*   DESCRIPTION
*      
*   PARAMETERS
*      ...
*   RETURNS
*      void
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_send_msg_to_aux
 * DESCRIPTION
 *  
 * PARAMETERS
 *  msg_name        [IN]        
 * RETURNS
 *  void
 *****************************************************************************/
void uem_send_msg_to_aux(kal_uint32 msg_name, ...)
{
#if 0 /* no AUX on MOLY*/
#ifdef __MTK_TARGET__
#if !defined(__SMART_PHONE_MODEM__) && !defined(__AUX_DISABLE__)
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
#endif /*  #if defined(__SMART_PHONE_MODEM__)  */    
#endif /* __MTK_TARGET__ */ 
/* under construction !*/
/* under construction !*/
#endif
}

#ifdef __USB_ENABLE__
/*****************************************************************************
*   Function 
*      uem_send_msg_to_usbms
*   DESCRIPTION
*      
*   PARAMETERS
*      ...
*   RETURNS
*      void
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_send_msg_to_usbms
 * DESCRIPTION
 *  
 * PARAMETERS
 *  msg_name        [IN]        
 * RETURNS
 *  void
 *****************************************************************************/
void uem_send_msg_to_usbms(kal_uint32 msg_name, ...)
{
#ifdef __MTK_TARGET__
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/
    kal_bool ret_val = KAL_TRUE;
    drvuem_usbcfg_req_struct *parm_stream;
    va_list varpars;
    local_para_struct *local_para_ptr = NULL;

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/

    switch (msg_name)
    {
        case MSG_ID_DRVUEM_USBCFG_REQ:
        {
            parm_stream = (drvuem_usbcfg_req_struct*) construct_local_para(sizeof(drvuem_usbcfg_req_struct), TD_CTRL);

            va_start(varpars, msg_name);
            parm_stream->mode = (USB_DEVICE_TYPE) va_arg(varpars, kal_uint32);
        #if (defined(__USB_IN_NORMAL_MODE__) && defined(__MULTI_BOOT__)) || defined(PICTBRIDGE_SUPPORT)
            if (POWER_ON_USB != uem_get_poweron_mode())
            {
                parm_stream->parameter = NULL; //(kal_uint32*) uem_normal_ms_buffer;
            }
        #endif /* (defined(__USB_IN_NORMAL_MODE__) && defined(__MULTI_BOOT__)) || defined(PICTBRIDGE_SUPPORT) */ 
            va_end(varpars);

            local_para_ptr = (local_para_struct*) parm_stream;
            break;
        }
        default:
            ret_val = KAL_FALSE;
            break;
    }

    /* Send the message to the UEM module */
    if (ret_val == KAL_TRUE)
    {
//        SEND_ILM(MOD_UEM, MOD_USB, DRIVER_PS_SAP, (&ilm_ptr));
        msg_send5(MOD_UEM, MOD_USB, DRIVER_PS_SAP, (msg_type)msg_name, local_para_ptr);
    }
#endif /* __MTK_TARGET__ */ 

    return;
}
#endif /* __USB_ENABLE__ */ 

#ifdef TST_INJECTED_EVENT
/*****************************************************************************
*   Function 
*      uem_send_msg_to_uem
*   DESCRIPTION
*      
*   PARAMETERS
*      ...
*   RETURNS
*      void
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_send_msg_to_uem
 * DESCRIPTION
 *  
 * PARAMETERS
 *  msg_name        [IN]        
 * RETURNS
 *  void
 *****************************************************************************/
void uem_send_msg_to_uem(kal_uint32 msg_name, ...)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/
    kal_bool ret_val = KAL_TRUE;
    local_para_struct *local_para_ptr = NULL;

#if (defined(__USB_ENABLE__) && defined(TST_INJECTED_EVENT))
    va_list varpars;
#endif 

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    kal_trace(TRACE_FUNC, FUNC_UEM_SEND_MSG_TO_UEM);

    switch (msg_name)
    {
        case MSG_ID_DRVUEM_KEYPAD_IND:
        {
            break;
        }

    #if (defined(__USB_ENABLE__) && defined(TST_INJECTED_EVENT))
        case MSG_ID_DRVUEM_USBDETECT_IND:
        {
            drvuem_usbdetect_ind_struct *param;

            param = (drvuem_usbdetect_ind_struct*) construct_local_para(sizeof(drvuem_usbdetect_ind_struct), TD_CTRL);

            param->action = (kal_uint8) va_arg(varpars, kal_uint32);
            local_para_ptr = (local_para_struct*) param;

            break;
        }
        case MSG_ID_DRVUEM_USBCFG_CNF:
        {
            drvuem_usbcfg_cnf_struct *param;

            param = (drvuem_usbcfg_cnf_struct*) construct_local_para(sizeof(drvuem_usbcfg_cnf_struct), TD_CTRL);

            va_start(varpars, msg_name);

            param->mode = (kal_uint8) va_arg(varpars, kal_uint32);
            param->result = (kal_uint8) va_arg(varpars, kal_uint32);
            param->reserved = (kal_uint8) va_arg(varpars, kal_uint32);

            va_end(varpars);

            local_para_ptr = (local_para_struct*) param;
            break;
        }
    #endif /* (defined(__USB_ENABLE__) && defined(TST_INJECTED_EVENT)) */ 

        default:
            ret_val = KAL_FALSE;
            /* SME_ASSERT(KAL_FALSE, "Wrong message" ); */
            break;
    }   /* End Switch */
    /* Check whether that there were no errors while constructing the message.  */

    /* Send the message to the UEM module */
    if (ret_val == KAL_TRUE)
    {
//        SEND_ILM(MOD_DRVKBD, MOD_UEM, UEM_L4C_SAP, (&ilm_ptr));
        msg_send5(MOD_DRVKBD, MOD_UEM, UEM_L4C_SAP, (msg_type)msg_name, local_para_ptr);
    }

    return;
}
#endif /* TST_INJECTED_EVENT */ 

/*****************************************************************************
*   Function 
*      uem_timer_init
*   DESCRIPTION
*      To initial the timer.
*   PARAMETERS
*
*   RETURNS
*      void
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_timer_init
 * DESCRIPTION
 *  
 * PARAMETERS
 *  void
 * RETURNS
 *  void
 *****************************************************************************/
void uem_timer_init(void)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    /* Create a no alignment timer schedule */
    event_scheduler_uem_ptr = evshed_create(
                                "UEM_Base_Timer1",
                                MOD_UEM,
                                0,
                                0);
}
#if 0
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
#endif

/*****************************************************************************
*   Function 
*      uem_start_timer
*   DESCRIPTION
*      To start base timer.
*   PARAMETERS
*      base_timer_ptr   IN   
*      time_out   IN
*   RETURNS
*      void
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_start_timer
 * DESCRIPTION
 *  
 * PARAMETERS
 *  nTimerId                    [IN]        
 *  timer_expiry                [IN]        
 *  nTimeDurationInTicks        [IN]        
 * RETURNS
 *  void
 *****************************************************************************/
void uem_start_timer(kal_uint8 nTimerId, UEM_TIMER_FUNC_PRT timer_expiry, kal_uint32 nTimeDurationInTicks)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/
    kal_uint16 temp = nTimeDurationInTicks / 10;

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    if (temp == 0)
    {
        temp = 1;
    }

    uem_timer_table[nTimerId].eventID = evshed_set_event(
                                            (event_scheduler*) event_scheduler_uem_ptr,
                                            (kal_timer_func_ptr) timer_expiry,
                                            NULL,
                                            (kal_uint32) (temp * KAL_TICKS_10_MSEC));
}

/*****************************************************************************
*   Function 
*      uem_stop_timer
*   DESCRIPTION
*      To start base timer.
*   PARAMETERS
*      base_timer_ptr   IN   
*      time_out   IN
*   RETURNS
*      void
*   GLOBALS AFFECTED
*****************************************************************************/


/*****************************************************************************
 * FUNCTION
 *  uem_stop_timer
 * DESCRIPTION
 *  
 * PARAMETERS
 *  nTimerId        [IN]        
 * RETURNS
 *  void
 *****************************************************************************/
void uem_stop_timer(kal_uint8 nTimerId)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    if (uem_timer_table[nTimerId].eventID != NULL)
    {
        evshed_cancel_event(
            (event_scheduler*) event_scheduler_uem_ptr,
            (eventid*) & (uem_timer_table[nTimerId].eventID));
        uem_timer_table[nTimerId].eventID = NULL;
    }
}


/*****************************************************************************
 * FUNCTION
 *  uem_send_msg_to_l1
 * DESCRIPTION
 *  
 * PARAMETERS
 *  msg_name        [IN]        
 *  data_ptr        [?]         
 * RETURNS
 *  void
 *****************************************************************************/
void uem_send_msg_to_l1(msg_type msg_id, void *local_data_ptr)
{
#if 0
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
#endif
    msg_send5(MOD_UEM, MOD_L1, EM_L1_SAP, msg_id, (local_para_struct*)local_data_ptr);

    return;
}


/*****************************************************************************
 * FUNCTION
 *  uem_genernal_send_msg
 * DESCRIPTION
 *  
 * PARAMETERS
 *  msg_name        [IN]        
 *  data_ptr        [?]         
 * RETURNS
 *  void
 *****************************************************************************/
void uem_genernal_send_msg(kal_uint16 msg_id, kal_uint32 src_mod, kal_uint16 dest_mod, void *data_ptr)
{
#if 0
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
#endif
    msg_send5((module_type)src_mod, (module_type)dest_mod, INVALID_SAP, (msg_type)msg_id, (local_para_struct*)data_ptr);
}


/*****************************************************************************
 * FUNCTION
 *  uem_set_gpio_status
 * DESCRIPTION
 *  
 * PARAMETERS
 *  gpio_dev_type       [IN]        
 *  gpio_dev_level      [IN]        
 * RETURNS
 *  void
 *****************************************************************************/
void uem_set_audio_loopback(audio_loopback_mode_enum mode)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    switch (mode)
    {
        case AUD_LOOPBACK_MODE_NORMAL:
	{
	    l4cuem_set_audio_mode(AUD_MODE_NORMAL);
	    kal_sleep_task(kal_milli_secs_to_ticks(200));
	    uem_set_loopback(KAL_FALSE);			
	    break;
        }
	case AUD_LOOPBACK_MODE_HEADSET_PREFER:
	{			
	    if (g_uem_cntx_p->ext_dev_status==EXT_DEV_HANDFREE || g_uem_cntx_p->ext_dev_status==EXT_DEV_EARPHONE)
	    {
	        uem_set_audio_loopback(AUD_LOOPBACK_MODE_HEADSET);
	    }
	    else
	    {
		uem_set_audio_loopback(AUD_LOOPBACK_MODE_LOUDSPK);			
	    }
	    break;
        }			
	case AUD_LOOPBACK_MODE_HEADSET:
	{			
	    l4cuem_set_audio_mode(AUD_MODE_HEADSET);
	    kal_sleep_task(kal_milli_secs_to_ticks(200));
	    uem_set_loopback(KAL_TRUE);
	    break;
        }			
	case AUD_LOOPBACK_MODE_LOUDSPK:
	{			
	    l4cuem_set_audio_mode(AUD_MODE_NORMAL);
	    kal_sleep_task(kal_milli_secs_to_ticks(200));
	    uem_set_loopback(KAL_TRUE);			
	    break;
        }			
	default:
	    break;
        }			
}

static void uem_do_set_gain(kal_uint32 type, void* gain)
{
	#if 0
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
/* under construction !*/
    #endif
}

static uem_gain_param_struct g_gain_param = {0};

void uem_set_gain(kal_uint8 type, kal_uint8 gain)
{
    media_in_proc_call_req_struct *msg;
    
    g_gain_param.gain = gain;
    
    msg = (media_in_proc_call_req_struct*) construct_local_para(sizeof(media_in_proc_call_req_struct), TD_CTRL);
    msg->func = uem_do_set_gain;
    msg->func_arg1 = (kal_uint32)type;
    msg->func_arg2 = (void*)&g_gain_param;
    
    uem_genernal_send_msg(MSG_ID_MEDIA_IN_PROC_CALL_REQ, MOD_UEM, MOD_MED, msg);
}

static void uem_do_set_loopback(kal_uint32 on, void* param)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
#if defined(__TC01__) && defined(__ACOUSTIC_LOOPBACK_SUPPORT__) && defined(__MTK_TARGET__)
    // Setup very short loopback length and use acoustic loopback instead
    L1SP_AcousticLoopbackLength(L1SP_DEFAULT_ACOUSTIC_LOOPBACK_LENGTH);
    
    if (on)
    {
        L1SP_AcousticLoopbackOn();
    }
    else
    {
        L1SP_AcousticLoopbackOff();
    }
#else
    L1SP_SetAfeLoopback((kal_bool)on);
    if (on)
    {
		  L1SP_Speech_On(0);
    }
    else
    {
    	L1SP_Speech_Off();
    }
#endif
}

void uem_set_loopback(kal_bool on)
{
    media_in_proc_call_req_struct *msg;
    
    msg = (media_in_proc_call_req_struct*) construct_local_para(sizeof(media_in_proc_call_req_struct), TD_CTRL);
    msg->func = uem_do_set_loopback;
    msg->func_arg1 = (kal_uint32)on;
    msg->func_arg2 = NULL;

    uem_genernal_send_msg(MSG_ID_MEDIA_IN_PROC_CALL_REQ, MOD_UEM, MOD_MED, msg);
}

/*****************************************************************************
 * FUNCTION
 *  uem_get_calibration_flag_req
 * DESCRIPTION
 *  
 * PARAMETERS
 *  void
 * RETURNS
 *  
 *****************************************************************************/
void uem_get_calibration_flag_req(void)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
#ifdef __TC01__
    uem_send_msg_to_nvram(MSG_ID_NVRAM_READ_REQ, NVRAM_EF_CAL_DATA_CHECK_LID, 0, 0);
#else
    uem_send_msg_to_nvram(MSG_ID_NVRAM_READ_REQ, NVRAM_EF_CAL_FLAG_LID, 0, 0);
#endif
}

/*****************************************************************************
 * FUNCTION
 *  uem_get_calibration_data_req
 * DESCRIPTION
 *  
 * PARAMETERS
 *  void
 * RETURNS
 *  
 *****************************************************************************/
void uem_get_calibration_data_req(void)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
#ifdef __TC01__
    uem_send_msg_to_nvram(MSG_ID_NVRAM_READ_REQ, NVRAM_EF_RF_CAL_ENV_LID, 0, 0);
#endif
}

/*****************************************************************************
 * FUNCTION
 *  uem_get_charging_complete_status
 * DESCRIPTION
 *  
 * PARAMETERS
 *  void
 * RETURNS
 *  
 *****************************************************************************/
kal_bool uem_get_charging_complete_status(void)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    return g_uem_cntx_p->is_charing_complete;
}

/*****************************************************************************
 * FUNCTION
 *  uem_get_charging_status
 * DESCRIPTION
 *  
 * PARAMETERS
 *  bmt_status      [IN]        
 * RETURNS
 *  void
 *****************************************************************************/
kal_bool uem_get_charging_status(void)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    return g_uem_cntx_p->is_charing;
}


/*****************************************************************************
 * FUNCTION
 *  uem_get_clam_status
 * DESCRIPTION
 *  
 * PARAMETERS
 *  void
 * RETURNS
 *  
 *****************************************************************************/
kal_bool uem_get_clam_status(void)
{
    /*----------------------------------------------------------------*/
    /* Local Variables                                                */
    /*----------------------------------------------------------------*/

    /*----------------------------------------------------------------*/
    /* Code Body                                                      */
    /*----------------------------------------------------------------*/
    return g_uem_cntx_p->clam_status;
}

/*****************************************************************************
 * FUNCTION
 *  uem_rtc_config
 * DESCRIPTION
 *  
 * PARAMETERS
 *  void
 * RETURNS
 *  
 *****************************************************************************/
void uem_rtc_config(kal_uint8 AL_EN, kal_uint8 TC_EN)
{
#ifdef __MMI_FMI__
    DCL_HANDLE hRtc;
    RTC_CTRL_CONFIG_AL_TC_T rtc_cmd_data2;

    hRtc = DclRTC_Open(DCL_RTC, FLAGS_NONE);
    if (hRtc == DCL_HANDLE_INVALID)
    {
        return;
    }
    
    rtc_cmd_data2.u1ALEn = AL_EN;
    rtc_cmd_data2.u1TCEn = TC_EN;

    DclRTC_Control(hRtc, RTC_CMD_CONFIG_AL_TC, (DCL_CTRL_DATA_T *)&rtc_cmd_data2);
    DclRTC_Close(hRtc);
#endif
}

/*****************************************************************************
 * FUNCTION
 *  uem_rtc_gettime
 * DESCRIPTION
 *  
 * PARAMETERS
 *  void
 * RETURNS
 *  
 *****************************************************************************/
void uem_rtc_gettime(RTC_CTRL_GET_TIME_T *time)
{
#ifdef __MMI_FMI__
    DCL_HANDLE hRtc;

    hRtc = DclRTC_Open(DCL_RTC, FLAGS_NONE);
    if (hRtc == DCL_HANDLE_INVALID)
    {
        return;
    }
    
    DclRTC_Control(hRtc, RTC_CMD_GET_TIME, (DCL_CTRL_DATA_T *)time);
    DclRTC_Close(hRtc);
#endif
}

/*****************************************************************************
 * FUNCTION
 *  uem_rtc_getaltime
 * DESCRIPTION
 *  
 * PARAMETERS
 *  void
 * RETURNS
 *  
 *****************************************************************************/
kal_bool uem_rtc_getaltime(RTC_CTRL_GET_ALARM_TIME_T *time)
{
#ifdef __MMI_FMI__
    DCL_HANDLE hRtc;
//    kal_bool result;
    
    hRtc = DclRTC_Open(DCL_RTC, FLAGS_NONE);
    if (hRtc == DCL_HANDLE_INVALID)
    {
        return KAL_FALSE;
    }

    DclRTC_Control(hRtc, RTC_CMD_GET_ALARM_TIME, (DCL_CTRL_DATA_T *)time);
    DclRTC_Close(hRtc);
#endif
    return KAL_TRUE;
}

/*****************************************************************************
 * FUNCTION
 *  uem_rtc_settime
 * DESCRIPTION
 *  
 * PARAMETERS
 *  void
 * RETURNS
 *  
 *****************************************************************************/
 void uem_rtc_settime(RTC_CTRL_SET_TIME_T *time)
{
#ifdef __MMI_FMI__
    DCL_HANDLE hRtc;

    hRtc = DclRTC_Open(DCL_RTC, FLAGS_NONE);
    if (hRtc == DCL_HANDLE_INVALID)
    {
        return;
    }

    DclRTC_Control(hRtc, RTC_CMD_SET_TIME, (DCL_CTRL_DATA_T *)time);
    DclRTC_Close(hRtc);
#endif
}

/*****************************************************************************
 * FUNCTION
 *  uem_rtc_setalarmtime
 * DESCRIPTION
 *  
 * PARAMETERS
 *  void
 * RETURNS
 *  
 *****************************************************************************/
void uem_rtc_setalarmtime(RTC_CTRL_SET_ALARM_TIME_T *time)
{
#ifdef __MMI_FMI__
    DCL_HANDLE hRtc;

    hRtc = DclRTC_Open(DCL_RTC, FLAGS_NONE);
    if (hRtc == DCL_HANDLE_INVALID)
    {
        return;
    }
    DclRTC_Control(hRtc, RTC_CMD_SET_ALARM_TIME, (DCL_CTRL_DATA_T *)time);
    DclRTC_Close(hRtc);
#endif
}

/*****************************************************************************
 * FUNCTION
 *  uem_rtc_istimevalid
 * DESCRIPTION
 *  
 * PARAMETERS
 *  void
 * RETURNS
 *  
 *****************************************************************************/
kal_bool uem_rtc_istimevalid(RTC_CTRL_IS_TIME_VALID_T *time)
{
#ifdef __MMI_FMI__
    DCL_HANDLE hRtc;

    hRtc = DclRTC_Open(DCL_RTC, FLAGS_NONE);
    if (hRtc == DCL_HANDLE_INVALID)
    {
        return KAL_FALSE;
    }
    DclRTC_Control(hRtc, RTC_CMD_IS_TIME_VALID, (DCL_CTRL_DATA_T *)time);
    DclRTC_Close(hRtc);
    return (kal_bool)(time->isTimeValid);
#else
    return KAL_FALSE;
#endif
}

/*****************************************************************************
 * FUNCTION
 *  uem_drv_power_off
 * DESCRIPTION
 *  
 * PARAMETERS
 *  void
 * RETURNS
 *  
 *****************************************************************************/
void uem_drv_power_off(void)
{
    DCL_HANDLE handle;
    
    handle=DclPW_Open(DCL_PW, FLAGS_NONE);
    DclPW_Control(handle, PW_CMD_POWEROFF, NULL);
    DclPW_Close(handle);
}

/*****************************************************************************
 * FUNCTION
 *  uem_drv_power_on
 * DESCRIPTION
 *  
 * PARAMETERS
 *  void
 * RETURNS
 *  
 *****************************************************************************/
void uem_drv_power_on(void)
{
    DCL_HANDLE handle;
    
    handle=DclPW_Open(DCL_PW, FLAGS_NONE);
    DclPW_Control(handle, PW_CMD_POWERON, NULL);
    DclPW_Close(handle);
}

/*****************************************************************************
 * FUNCTION
 *  uem_drv_set_power_on_mode
 * DESCRIPTION
 *  
 * PARAMETERS
 *  void
 * RETURNS
 *  
 *****************************************************************************/
void uem_drv_set_power_on_mode(kal_uint8 mode)
{
    DCL_HANDLE handle;
    PW_CTRL_SET_POWERON_REASON CtrlVal;
    
    CtrlVal.powerOnReason = (PW_CTRL_POWER_ON_REASON)mode;

    handle=DclPW_Open(DCL_PW, FLAGS_NONE);
    DclPW_Control(handle, PW_CMD_SET_POWERON_REASON,(DCL_CTRL_DATA_T *)&CtrlVal);
    DclPW_Close(handle);
}

/*****************************************************************************
 * FUNCTION
 *  uem_drv_switch2idle_flag
 * DESCRIPTION
 *  
 * PARAMETERS
 *  void
 * RETURNS
 *  
 *****************************************************************************/
void uem_drv_switch2idle_flag(void)
{
    DCL_HANDLE handle;

    handle=DclPW_Open(DCL_PW, FLAGS_NONE);
    DclPW_Control(handle, PW_CMD_SET_SWITCH_TO_IDLE, NULL);
    DclPW_Close(handle);
}

void uem_uart_turnon_power(UART_PORT port, kal_bool on)
{
    DCL_HANDLE handle;
    UART_CTRL_POWERON_T data;
    
    handle = DclSerialPort_Open(port, 0);
    data.bFlag_Poweron = on;
    DclSerialPort_Control(handle, UART_CMD_POWER_ON, (DCL_CTRL_DATA_T*)&data);
    
    DclSerialPort_Close(handle);
}

#ifdef __VIBRATION_SPEAKER_SUPPORT__
#include "aud_main.h"

static void uem_gpio_spkvib_enable_callback( kal_uint32 arg1, void* arg2 )
{
    Media_SpkVibration_Enable((kal_bool)arg1);
}

void uem_gpio_spkvib_enable(kal_bool enable)
{
    aud_send_in_proc_call_req(MOD_UEM, uem_gpio_spkvib_enable_callback, (kal_uint32)enable, NULL);
}
#endif

